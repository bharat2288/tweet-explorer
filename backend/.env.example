# OpenAI API key (required for semantic search + LLM analysis)
OPENAI_API_KEY=sk-your-key-here

# LLM configuration (optional, defaults shown)
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o

# Anthropic API key (only needed if LLM_PROVIDER=anthropic)
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# Server
HOST=0.0.0.0
PORT=8400
